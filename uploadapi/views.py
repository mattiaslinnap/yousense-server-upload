
@csrf_exempt
def upload2(appid, installid):
    # Check client version.
    # Validate upload metadata: SHA1, block identifiers.
    # Save data to DB.

@csrf_exempt
def status2(appid, installid):
    # Check client version.
    # Parse data, validate
    # Save status.